const config = {
  formsparkFormId: process.env.NEXT_PUBLIC_FORMSPARK_FORM_ID,
  hCaptchaSiteKey: process.env.NEXT_PUBLIC_HCAPTCHA_SITE_KEY,
  gaTrackingId: process.env.NEXT_PUBLIC_GA_TRACKING_ID,
};

export default config;
